export const trianglePerimeter = (base, height, hypotenuse) => {
  return parseFloat(base) + parseFloat(height) + parseFloat(hypotenuse);
};

export const triangleArea = (base, height) => {
  return (base * height) / 2;
};
